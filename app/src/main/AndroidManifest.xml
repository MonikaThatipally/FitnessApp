<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thatipallymonika.fitness">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BMImetric" />
        <activity android:name=".BMIus" />
        <activity android:name=".DWIcalc" />
        <activity android:name=".BMRus" />
        <activity android:name=".Idealweight" />
        <activity android:name=".DCIcalc" />
        <activity android:name=".WaterUs" />
        <activity android:name=".IdealUs"></activity>
    </application>

</manifest>